```c++
#include <iostream>
#include <string>
#include <queue>
#include <deque>
#include <tuple>
#include <algorithm>

using namespace std;

// 시작시간 : 1538
// 맞은시간 : 1549

int main(void) {
    int t; cin >> t;
    
    while (t--) {
        int x1, y1; cin >> x1 >> y1;
        int x2, y2; cin >> x2 >> y2;
        
        int n; cin >> n;
        int count = 0;
        for(int i=0; i < n; i++){
            int cx, cy , r; cin >> cx >> cy >> r;
            bool ok = false;
            if((r * r) < ((cx-x1) * (cx-x1)) + ((cy-y1) * (cy-y1))) ok = !ok;
            if((r * r) < ((cx-x2) * (cx-x2)) + ((cy-y2) * (cy-y2))) ok = !ok;
            if(ok) count++;
        }
        cout << count << "\n";
        
    }
    return 0;
}
```
