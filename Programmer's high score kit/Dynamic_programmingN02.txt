정수삼각형 10DEC  PM 12:54
-문제이해(3"50)
-문제해결방법(18"00) + 40" + 30"(교재 참고)
-손코딩 20"
-구현 8"
-디버깅 9"


위와 같은 삼각형의 꼭대기에서 바닥까지 이어지는 경로 중 , 거쳐간 숫자의 합이
가장 큰 경우를 찾아보려고 합니다. 아래 칸으로 이동할 때는 대각선 방향으로 한칸 오른쪽
또는 왼쪽으로만 이동 가능합니다. 예를 들어 3에서는 그 아래칸의 8 또는 1로만 이동이 가능합니다.

삼각형의 정보가 담긴 배열 triangle이 매개변수로 주어질때, 거쳐간 숫자의 최댓값을 return



#문제이해
꼭대기부터 바닥까지 대각 아래로 좌우 이렇게 내려온다.
그 경로 사이 합이 가장 큰값을 return


#문제해결방법
동적계획
꼭대기부터 아래까지 더해오는 과정을 별도 공간에 기록하여 중복제거
어떻게 부분으로 쪼갤 것인가?
제일 작은 삼각형
 7
3 8

삼각형으로 쪼갰을때 
정점에서 밑으로 내려오는 과정을 반복 
바닥이 나올때까지 왼쪽으로 내려가거나 오른쪽으로 내려가거나 
정점별 count값? X
같은 정점이어도 이전에 어떤 경로를 거쳤는지에 따라 값이 다르다.
이전과 같은 경로라면 그 값을 넘겨준다.
어떻게?
어떻게 같은 경로였는지 알것인가?
경로를 기록한다?
더 단순한 방법은?


- 모든 경우의 수는 어떻게 만들 것인가?
  그리고 그 경우들을 어떻게 비교할 것인가?
  그냥 재귀로 max(ㅁ, ㅁ)
  이런식으로 처리 될 수 있는지 확인

어떻게 작은 문제로 쪼갤 수있는가
맨 위에서 바닥까지 찍어야한다.
바닥까지 도달.
이전 경로는 저장해야 중복을 피할 수 있다.
이전 경로를 어떻게 저장할 것인가?
높이 3짜리에서 바닥 바로 위까지 간 경우 "0_0", "0_1"과 같은 방법으로 인덱스를 '_'로 연결?
그렇게 경로를 저장한다고 쳤을때 
모든 경우의 수는 어떻게 만들것인가?
꼭대기에서 바닥까지 가는 경우를 
중간 노드는 겹치더라도 경로가 다름을 어떻게 알 것인가?
작은 문제로 쪼개면 삼각형 한개로 본다?
삼각형 1개(높이 2짜리 3개 노드로 연결된 모습)로 쪼개면?
현재 내 노드에서 거쳐온 총합이 큰 값을 선택
나에게 오기 이전 노드의 값이 "둘 중"더 큰 값을 선택한다.

계산된 결과(이전 노드까지의 합 중 큰 값이 선택된 값)는 별도 배열에 저장

어떻게 반복문을 돌릴것인가?
밑바닥 행의 모든 원소값이 완성될때까지
그걸 어떻게 알 것인가?
같은 조건이면서 더 쉽게 할 수 있는 방법
최대값을 저장할 별도 배열 모든 원소의 초기값을 -1로 만들며
모든 값이 -1인 동안 순환되게 한다?
- 삼각형 모양만큼 반복
삼각형의 높이 정보를 이용한다.
1부터 최대 높이까지 
꼭대기는 1
바로 밑에 층은 꼭대기+1
밑바닥층은 높이와 같은 원소 개수
가생이 쪽은 바로 위의 값만 더하기 가능 
현재 층의 0번 인덱스는 위 층에 0번 인덱스 
현재 층 마지막 인덱스는 위치의 마지막 인덱스
그 이외에 사이에 있는 인덱스들은 위치 본인 인덱스, 인덱스+1 위치의 인덱스 값중 큰 값을 선택하면 됨


-----------

1.최적해의 구조의 특징을 찾는다.
높이 2까지 삼각형으로 생각?
나라는 현재 정점에 이르기 위해서는 왼쪽 위, 오른쪽 위 두 값중 최대값을 선택하면된다.
왼쪽 위, 오른쪽 위의 값은 마찬가지로 자신의 왼쪽 위, 오른쪽 위 값 중 최대값을 선택.
꼭대기부터 시작해야 가능
왼쪽 위 오른쪽 위가 있는지 확인 

2.최적해의 값을 재귀적으로 정의한다.
3.최적해의 값을 계산한다.
4.계산된 정보들로부터 최적해를 구성한다.



base case는 무엇인가


#실행
t_w 배열을 초기화하는 부분에 문제가 있어
디버깅 소요됨
초기단계부터 잘보았다면 문제없었을 것


#회고
- 문제 이해가 더 잘되었다면 수월했을 것
문제 이해하는 시간을 더 투자한다.
문제를 이해하고 2가지 이상의 버젼으로 
나만의 언어로 문제를 재정의.

- 해결방법을 생각하는 과정에서 첫번쨰 방법으로 생각하다 시간을 허비
- 좀더 밀도있게 생각할 것

